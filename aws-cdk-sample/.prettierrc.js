/**
 * コードを自動整形してくれる。
 * 下記で開発環境にインストール
 * ```
 * npm install -D prettier eslint-config-prettier eslint-plugin-prettier
 * ```
 */
module.exports = {
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'es5',
}